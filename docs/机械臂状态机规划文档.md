我认同：先把真机运行状态机定义清楚，再讨论代码实现。

先修正两个关键点：

- “异常后保持当前动作不动”应准确表述为：**立即终止当前轨迹，然后保持当前实测关节位置**，不能让原动作继续执行。
- 节点重启后不建议自动回 STOWED。此时机械臂可能持物、旁边可能有人。应先预检；如果不在 STOWED，则进入故障保持状态，由操作者显式调用恢复接口。

恢复接口建议命名为：

```text
/arm/tasks/recover_to_stowed
```

它应该是 ROS 2 Action，因为包含规划、执行、反馈和可能的失败，不适合做瞬时 Service。

## 一、状态定义

采用以下九个状态：

| 简写 | 状态 | 语义 |
|---|---|---|
| NR | `NOT_READY` | 配置或预检未通过，不接受任务 |
| RS | `READY_STOWED` | 机械臂已收纳，允许 Pick |
| PK | `PICKING` | 正在执行完整抓取流程 |
| CY | `CARRYING` | 已确认持物并处于 CARRY，允许 Drop |
| DP | `DROPPING` | 正在执行投放流程 |
| FH | `FAULT_HOLD` | 发生异常，轨迹已停止，位置保持已确认 |
| RC | `RECOVERING` | 正在执行 `recover_to_stowed` |
| CL | `CONTROL_LOST` | 控制或反馈丢失，无法确认机械臂是否保持 |
| ES | `E_STOPPED` | 物理急停或硬件安全停机已触发 |

必须区分 `FAULT_HOLD` 和 `CONTROL_LOST`：

- `FAULT_HOLD`：系统知道机械臂停在哪里，并确认控制器正在保持。
- `CONTROL_LOST`：系统连“保持不动”都无法确认，不能假装安全。

## 二、N × N 状态转移表

空白表示禁止转移。

| 当前 \ 目标 | NR | RS | PK | CY | DP | FH | RC | CL | ES |
|---|---|---|---|---|---|---|---|---|---|
| **NR** | 预检未通过，更新错误详情 | 预检通过且实测为 STOWED |  |  |  | 预检通过但不在 STOWED，且位置保持已确认 |  |  | 物理急停触发 |
| **RS** | 空闲时相机、TF、IMU等任务前提失效 |  | 接受 `pick_object` |  |  | 姿态异常且停止保持成功 |  | 控制器或关节反馈丢失 | 物理急停触发 |
| **PK** |  |  |  | Pick 全流程成功且持物复检通过 |  | 取消、超时或失败，且停止保持成功 |  | 无法停止或控制反馈丢失 | 物理急停触发 |
| **CY** |  |  |  | Drop 前置条件暂时不满足，拒绝请求并继续持物 | 接受 `drop_object` | 持物状态出现异常但保持成功 |  | 控制器或关节反馈丢失 | 物理急停触发 |
| **DP** |  | 投放成功并回到 STOWED |  | 尚未运动即确定需调整 Go2 位置 |  | 取消、超时或执行失败，且停止保持成功 |  | 无法停止或控制反馈丢失 | 物理急停触发 |
| **FH** |  |  |  |  |  | 非恢复接口全部拒绝；恢复前提不满足时保持原状态 | 接受 `recover_to_stowed` | 保持过程中控制链路丢失 | 物理急停触发 |
| **RC** |  | 恢复成功并验证 STOWED |  |  |  | 恢复失败或取消，但当前位置保持成功 |  | 恢复中控制链路丢失 | 物理急停触发 |
| **CL** | 控制链路恢复、人工确认后重新预检 |  |  |  |  |  |  | 等待链路恢复和人工处理 | 物理急停触发 |
| **ES** | 物理急停解除、人工确认并重新预检 |  |  |  |  |  |  |  | 急停保持 |

## 三、通用失败处理机制

不需要针对每种错误设计单独恢复流程，统一按以下规则处理。

### 1. 请求尚未被接受

例如：

- 状态不允许；
- 坐标非法；
- 必要 TF 不存在；
- 当前不是 STOWED 却调用 Pick；
- 当前不是 CARRYING 却调用 Drop。

处理方式：

- 直接拒绝请求；
- 状态不变；
- 返回明确原因；
- 不进入故障状态。

### 2. 任务已经开始，但尚未产生运动

例如 Drop 规划后发现垃圾桶需要 Go2 重新靠近。

处理方式：

- 返回 `REPOSITION_REQUIRED`；
- 保持原稳定状态；
- Pick 一般回到 `FH`，因为可能已经运动到了观测姿态；
- Drop 如果尚未离开 CARRY，则回到 `CY`，允许 Go2 调整后重试。

这是“预期内无法完成”，不算控制故障。

### 3. 任务运动已经开始，随后异常

统一执行：

1. 停止产生新轨迹；
2. 取消 MoveIt 和底层控制目标；
3. 使用最新实测关节位置下发保持；
4. 等待机械臂速度降到阈值内；
5. 如果停止和保持得到确认，进入 `FAULT_HOLD`；
6. 如果无法确认，进入 `CONTROL_LOST`；
7. 在 `FAULT_HOLD` 中只接受 `recover_to_stowed`。

### 4. 物理急停

任何状态下直接进入 `E_STOPPED`。解除急停后也不能直接恢复任务，必须重新进行完整预检。

## 四、`recover_to_stowed` 的语义

这个接口只做一件事：从当前实测姿态规划并执行到 STOWED。

它遵循以下规则：

- 只在 `FAULT_HOLD` 中接受。
- 不自动打开或关闭夹爪。
- 保持当前夹爪命令。
- 如果 MoveIt 中存在 attached object，则带着该碰撞体规划。
- 使用机械臂自身、Go2、地面和已知 attached object 做碰撞检查。
- 规划失败时不强行走关节插值，继续停留在 `FAULT_HOLD`。
- 执行失败但能够保持时，回到 `FAULT_HOLD`。
- 控制失联时进入 `CONTROL_LOST`。
- 到达 STOWED 后连续检查若干帧关节状态，确认稳定后才进入 `READY_STOWED`。

调用这个接口相当于操作者明确确认：“当前环境允许机械臂尝试回收”。系统本身不猜测是否安全。

## 五、节点重启行为

不自动运动，按以下流程：

```text
节点启动
   |
   v
NOT_READY：加载配置并执行预检
   |
   +-- 实测已在 STOWED ----------> READY_STOWED
   |
   +-- 不在 STOWED，能够可靠保持 -> FAULT_HOLD
   |
   +-- 无法取得可靠控制反馈 ------> 保持 NOT_READY
   |
   +-- 物理急停已触发 ------------> E_STOPPED
```

这样既不会凭空假设 STOWED，也不会在进程重启后突然运动。

## 六、仿真/真机配置怎么划分

机械臂与腕部相机系统可以作为一个完整的 ROS 2 子系统交付，但不建议塞进一个单独节点。更合理的是由一个 bringup 启动多个内部节点，对机器狗团队只暴露统一接口：

```text
arm_bringup
├── D1 hardware bridge
├── RealSense driver/adapter
├── Go2 gravity adapter
├── perception
├── MoveIt/ros2_control
└── manipulation supervisor
```

### 由我们维护，不交给机器狗团队修改

这些属于机械臂子系统内部契约：

- D1 七个关节的顺序、方向和安全限位；
- D1 官方命令与反馈话题名称；
- 机械臂控制频率；
- STOWED、CARRY 等固定姿态；
- 三类物体的夹爪目标和脱落阈值；
- Link6 到腕部 RGB 相机的标定外参；
- RGB、Depth、CameraInfo 对外统一话题名称；
- RealSense 分辨率、格式、对齐方式；
- MoveIt 参数；
- 感知与抓取参数；
- 各阶段默认超时。

这些可以放在我们维护的只读配置文件中，但不建议写成散落的代码常量。

### 由机器狗团队在部署时配置

这些与整机部署环境有关：

- 真机网卡名称；
- D1 DDS domain；
- 整机 ROS domain 和 namespace；
- Go2 LowState/IMU 话题名称；
- Go2 base frame 名称；
- `go2_base → arm base_link` 安装变换；
- 时间同步方案；
- 腕部 RealSense 序列号或设备路径。

尤其是 RealSense 序列号需要可配置，因为机器狗自身还会有另一台 RealSense，不能依赖“系统发现的第一台设备”。

### 由我们生成、随具体设备交付的标定文件

这些不是机器狗团队随意填写，也不适合硬编码：

- 当前腕部相机的序列号；
- `T_Link6_RGB`；
- Joint6 角度到夹爪开度的映射；
- 必要时的关节零位修正；
- 相机标定版本和日期。

相机内参应优先读取实时 `CameraInfo`，不应把某一台 D435i 的内参写死。

## 七、第一版超时建议

先采用偏宽裕的参数：

| 环节 | 建议超时 |
|---|---:|
| 启动整体预检 | 20 s |
| 等待第一帧关节反馈 | 10 s |
| 运行中关节反馈最大年龄 | 0.5 s |
| 等待 LowState/IMU | 5 s |
| 运行中 IMU 最大年龄 | 0.25 s |
| 等待 TF 建立 | 5 s |
| 动态重力 TF 最大年龄 | 0.25 s |
| 等待第一组 RGB-D | 10 s |
| 运行中图像最大年龄 | 0.5 s |
| RGB 与 Depth 最大时间差 | 50 ms |
| 等待内部 Service/Action | 5 s |
| 单次 YOLO/感知请求 | 15 s |
| 单次 MoveIt 规划 | 5 s |
| 完整观测姿态搜索 | 60 s |
| Pick 总体外层超时 | 120 s |
| Drop 总体外层超时 | 60 s |
| 夹爪单次动作 | 8 s |
| 停止并确认位置保持 | 1.5 s |
| 恢复路径规划 | 10 s |
| 恢复轨迹执行 | 规划轨迹时长 × 2 + 5 s |

这些值先保证联调不容易误超时，拿到真机运行统计后再按 P99 耗时加安全余量收紧。