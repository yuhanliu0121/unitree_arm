# Arm Collection 项目：Codex CLI Handoff

> 更新时间：2026-08-24
> 用途：把此前在 ChatGPT 中已经完成的讨论结论移交给本地 Codex CLI。  
> 当前阶段：MuJoCo、ros2_control、MoveIt 固定方块抓取基线已打通。

## 0. 当前实现状态（优先于下文的早期规划描述）

当前工作区已经实现并验证：

- 2026-08-23 已实现新的真机关节流式执行链：MoveIt 继续使用标准
  `joint_trajectory_controller` 与 `gripper_action_controller`，
  `D1SystemHardware` 以 20 Hz 采样控制器目标，隔离 DDS gateway 把每个完整
  七关节快照转换成同序列号的 `set_servo_angle` 消息；新增板端
  `unitree-d1-streaming-control` 板端执行器将七条消息成组后交给唯一串口线程连续下发，并在
  同一线程发布约 10 Hz 反馈。该设计复用 ROS 2 Control 的轨迹插值、容差、
  Action 和取消语义，只自行实现 D1 SDK 缺失的批量关节传输部分。
- 主机代码已编译，仿真固定方块抓取回归通过；板端程序已在 D1 计算机的
  `/tmp` 中编译通过，但**尚未替换厂商 `marm_controller.service`，也尚未做
  真机运动验证**。部署前必须备份厂商程序并准备一键回滚。

- 以下为上一版真机控制链记录，现仅作为回滚背景：`d1_mode1_controller` 同时实现 MoveIt 使用的
  `/arm_controller/follow_joint_trajectory` 与
  `/gripper_controller/gripper_cmd`，是 Joint0～6 唯一运动命令所有者；
  所有真机动作均发送完整 `funcode=2, mode=1` 七关节快照。机械臂目标只
  更新 Joint0～5 并保留夹爪期望值，夹爪目标只更新 Joint6 并保留机械臂
  期望值。真机 ros2_control hardware interface 仅负责上电/使能预检和反馈，
  运动写出口已关闭；仿真仍使用标准 arm/gripper controllers。
- D1095 已真机验证：在 Joint0～5 保持当前角度时，以
  `funcode=2, mode=1` 将 Joint6 在 60° 与 -30°之间开合，六轴保持稳定、
  夹爪平滑、反馈不中断。此前精观测后失能的直接原因是直接六轴执行器与
  ros2_control 夹爪链路同时写入完整七关节命令，导致旧六轴缓存被重发；
  新的唯一命令所有者消除了该双写路径。

### 真机已知问题：多关节目标偶发仅部分执行

- 2026-08-23 在 D1095 立方体抓取的 `LIFT` 阶段复现一次：MoveIt 已成功
  规划并把完整七关节 `funcode=2, mode=1` 目标送达，D1 也返回接收与执行
  ACK，但实际只有部分关节运动。
- 该次六轴初始角为
  `[-36.0, 28.0, 23.4, 2.1, 39.8, -37.8] deg`，目标角为
  `[-35.65, 17.56, 5.15, 1.35, 68.78, -36.31] deg`；约 14 秒后反馈停在
  `[-35.7, 18.3, 23.4, 2.0, 39.8, -37.8] deg`。Joint1 基本到位，
  Joint2 与 Joint4 几乎未执行，最终触发 endpoint timeout。
- 反馈链全程存在，因此现有证据不支持普通网络丢包；问题更接近 D1 固件对
  多关节 `mode=1` 目标的偶发部分执行，厂商 ACK 也不能证明所有关节最终
  到位。
- 当前仅记录，不增加自动恢复或新的控制补丁。后续应统计复现率，并基于
  逐关节终点反馈实现有界重发；在明确重发安全语义前，异常后继续保持当前
  姿态。

### 真机已知问题：释放候选缺少执行误差下的碰撞余量

- 2026-08-23 连续多次从 STOWED 空载调用 `drop_object` 时，大部分成功，
  其中一次成功到达释放位姿、张开夹爪后，MoveIt 拒绝规划回 STOWED。
- 该次释放关节目标为
  `[0.20, 18.44, 31.64, -0.83, 38.25, 6.69] deg`，真机到位反馈为
  `[-0.30, 17.10, 32.30, -0.60, 38.40, 6.70] deg`；最大误差约 1.34 deg，
  但实际反馈状态下手指已与 `go2_platform` 规划碰撞体相交。
- 当前释放策略选择第一个完整规划成功的 IK 解，没有验证关节到位容差邻域
  内的碰撞余量，也没有在执行前预验证“张开夹爪后返回 STOWED”的回程。
- 当前仅记录，留待统一异常/失败处理阶段解决。后续应验证完全张开的夹爪
  状态、给 Go2 碰撞留出到位误差余量，并在执行释放前验证完整往返方案；
  Action 失败结果也应保留实际选择的 release pose 供诊断。

### 真机已知问题：接触阻挡后轨迹超时并停留在非安全任务姿态

- 2026-08-24 在 D1095 立方体抓取的 `DESCEND` 阶段复现：MoveIt 已生成
  完整笛卡尔下降轨迹并下发终点，但 Joint3 未到位，目标约 `-3.9 deg`，
  最终反馈约 `+5.4 deg`；MoveIt 等待约 8 秒后以执行超时结束任务。
- 操作者观察到抓手没有正确对准，下降时 TCP/抓手受到立方体阻挡。结合
  反馈判断，接触阻挡是 Joint3 未到位的优先解释，但当前没有力矩、关节电流
  或接触传感器证据，因此该因果关系仍属于待验证推测，而非已确认事实。
- 当前行为是停止继续执行抓取、保持失败时的非 STOWED 姿态，不自动后退，
  也不自动返回 STOWED。该行为避免在未知接触状态下盲目恢复，但实际任务中
  会留下机械臂受载、物体接触和任务状态不确定的问题，需要人工介入。
- 当前仅记录，纳入后续统一异常/失败处理：区分接触阻挡、控制器未执行和
  普通终点误差；记录各关节目标/反馈与超时阶段；定义可靠的停止语义、允许
  调用的恢复接口，以及何时可以执行有界撤退或返回 STOWED。完成该设计前，
  不在超时回调中自动下发新的姿态目标。

### 2026-08-23 cube 真机阶段结论

- `pick_object` 已完成从 STOWED、粗观测、精观测、PREGRASP、GRASP、LIFT
  到 CARRY 的真机多轮验证；粗观测只从 `yellow_cube`、`zucchini`、`bowl`
  三类 `depth_verified` 结果中选择满足置信度阈值且距 RGB 光心最近的目标。
- `drop_object` 的正常投放链路已在真机和空载重复运行中验证，但上述 D1
  偶发部分执行与释放候选碰撞余量问题暂不在本阶段扩展恢复机制。
- cube 抓取测试阶段至此结束；下一阶段转入 zucchini 真机抓取验证。

- `d1_mujoco_sim`：D1 SDK 协议兼容的 MuJoCo 服务，DDS domain 42；
- `d1_ros2_control`：隔离 SDK ABI 的 UDP gateway、hardware interface，以及
  `joint_state_broadcaster`、`arm_controller`、`gripper_controller`；
- `d1_moveit_config`：SRDF、KDL IK、OMPL、控制器映射和规划专用简化碰撞体；
- `d1_grasp_demo`：读取 seed-0 方块仿真真值，执行“预抓取—垂直下降—
  双侧接触检查—闭合—垂直抬升—保持验证”。
- eye-in-hand D435i：官方 CAD 和 `main_stand` 支架可视模型、RGB/Depth 独立光学 frame、真机
  内参、实测 Link6→RGB 外参和本机 factory Depth→RGB 外参均已进入
  MuJoCo；固定相机名为 `d435i_color`、`d435i_depth`。
- Go2 平台：官方 `stand_down` mesh 已按手工 Blender 装配结果导入
  MuJoCo。标定关系为 `T_go2_base_d1_base` 平移
  `[0, 0, 0.057961769402] m`、旋转为单位四元数。Go2 被明确简化成
  固定趴卧外观、一个整机 bounding box 和仅允许 GT `x/y` 平移的
  mocap 母体；不模拟腿关节、站立、步态、yaw 或避障。
- Go2 机械臂作业趴姿已固化到
  `calibration/go2_d1_mount/go2_manipulation_posture.yaml`：采用官方
  `stand_down_joint_pos`，记录了 SDK 12 电机顺序、`ground→go2_base`、
  `go2_base→D1 base_link` 和真机待验收字段；面向运控的交接说明位于同目录
  `LOCOMOTION_HANDOFF_CN.md`。官方关节目标已验证全部位于当前 Go2 URDF
  限位内，但安装 D1 后的稳定阈值仍必须由运控真机验证。

2026-08-11 加入 Go2 后的端到端回归结果：

- Go2 mesh 最低点贴地，D1、相机和全部机械臂 frame 随 Go2 母体移动；
- 仿真真值在发送给 MoveIt 前转换为 `base_link` 规划坐标，地面相对高度
  同步调整；
- 固定方块从新位置完成抓取，闭合后左右法向力约 `7.50 N / 7.50 N`；
- 抬升 0.10 m 后保持 12 秒，最终高度增量 `0.099 m`，验收通过；
- `d1_mujoco_sim` 共 42 项 pytest 全部通过。

2026-08-11 目标引导观测第一阶段结果：

- 新增 `d1_manipulation` 和调试 Action `/arm/debug/observe_target`；
- 在实时重力法平面中，以固定 `beta → alpha → distance` 顺序生成 441 个
  相机候选，不使用加权评分；
- 每个候选通过手眼外参换算为 `Link6` 位姿，顺序执行 IK、限位、碰撞和
  MoveIt 完整轨迹规划，执行第一个全部成功的候选；
- seed-0 方块在第 56 个候选 `beta=0°、alpha=65°、distance=0.60 m`
  找到首条可行轨迹并成功执行；
- 执行后使用实时 TF、D435i CameraInfo 和 `plumb_bob` 参数复核，目标距
  RGB 图像中心 `0.3 px`；
- 候选几何 3 项 gtest、无 GUI 端到端和 `--headless --rviz` 联合启动均通过；
- 一键入口为 `./accept_observe_target.zsh`，`--server-only` 用于另一个终端
  手动发送 Action。

2026-08-10 的端到端验收结果：

- TCP 与方块中心对齐误差小于 1 mm；
- 闭合后左右手指均有接触，法向力约为 7.50 N / 7.50 N；
- TCP 规划抬升 0.10 m，保持 12 秒期间物理方块稳定在初始位置上方
  0.097 m，没有继续下滑；
- `pick_cube` 正常输出 `GRASP SUCCEEDED` 并以 0 退出；
- `d1_mujoco_sim` 共 34 项 pytest 全部通过。

重要边界：MoveIt 使用启动时生成的 primitive collision URDF；共享 vendor
URDF 保留 STL，以免破坏 MuJoCo 导入时的 link-frame 变换。为避免在已知
不代表真机能力的仿真打滑上继续耗时，当前任务级仿真明确采用强稳定参数：
夹爪 `kp/kv=800/80`、闭合目标越过物理下限 5 mm、指垫滑动摩擦系数
`5.0`、指垫—物体接触时间常数 2 ms，以及物体线性/角阻尼 `2.0/4.0`。
这些只用于稳定验证规划和抓取流程，不能直接视为真机参数；
双指 mimic equality 同样采用 2 ms 高刚度约束，避免单侧接触载荷造成开合
不对称。
此外启用 MuJoCo 椭圆摩擦锥、`impratio=10` 和 2 次 NoSlip 迭代，以消除
软接触模型在接触力充足时仍会产生的慢速摩擦漂移。这仍是正常接触求解，
没有焊接、附着或强制跟随物体。
真机前仍须根据 D1 实测力/电流重新拟合动力学参数。

仿真相机 ROS 2 感知桥已经实现，目标发布频率配置为 10 Hz：发布与 RealSense 官方
驱动同语义的 RGB `rgb8`、原始 Depth `16UC1`、对齐到 RGB 的 Depth
`16UC1`、三份 `CameraInfo` 和静态 TF。机械臂相机统一使用
`/wrist_camera` namespace 及 `wrist_camera_*` frames，以便与未来 Go2
机身相机区分。RGB 图会应用实测 `plumb_bob` 畸变；
原始 `16UC1` 深度有效范围固定为 0.20—10.0 m，范围外发布为零；彩色
调试图固定把 0.20—2.0 m 映射到 plasma 色带，范围外显示为黑色。
Plasma 话题位于 `/wrist_camera/debug`，由仿真和真机共用的
`d1_camera_visualization/depth_debug_visualizer` 从原始 `16UC1` 生成；
相机渲染、对齐和 ROS 序列化使用 latest-only 后台线程，物理/控制循环只复制
最新 `qpos` 快照，避免完整 RViz 图像链阻塞 1 kHz 仿真。完整
`--headless --rviz` 抓取回归已通过，12 秒保持高度仍为 0.097 m；
`./view_mujoco_camera.zsh` 保留为只检查相机的工具。固定方块完整验收统一使用
`./accept_cube_grasp.zsh`，加 `--rviz` 时会同时显示机械臂、MoveIt、RGB 和
对齐彩色深度视图，原始深度调试视图默认关闭但可手动开启。

MuJoCo→RViz 场景可视化桥已经实现：`Object Meshes` 以 25 Hz 显示三个
原始 OBJ 并持续跟随 MuJoCo 真值；`MuJoCo Physical Collisions` 直接遍历
编译后模型中实际启用接触的 geom；`MoveIt Planning Collisions` 独立显示
规划场景。RViz 默认开启机械臂、物体 mesh 和两个相机面板，默认关闭两套
碰撞层；两套碰撞层打开时地面均不透明。固定方块 mesh 不再受 MoveIt
移除/附着生命周期影响。

下一步最值得做的是由用户在 `--rviz` 下人工验收最终观测姿态、RGB/Depth
画面和 Observation Debug 标记；通过后再实现 RGB-D 黄色方块位姿估计，
用感知结果替换固定真值，同时保持现有 MoveIt 抓取执行链不变。真机部署前
还需实现 `LowState` 重力适配器和真实地面平面碰撞输入。

日常固定方块验收已经封装为工作区根目录的单命令脚本：

```bash
./accept_cube_grasp.zsh                   # MuJoCo GUI
./accept_cube_grasp.zsh --headless        # 无 GUI
./accept_cube_grasp.zsh --rviz            # MuJoCo GUI + 完整 RViz
./accept_cube_grasp.zsh --headless --rviz # 仅完整 RViz
```

两个参数相互独立，四种组合都会执行抓取。脚本负责启动、就绪检测、成功
字符串与退出状态检查，以及后台进程清理。交互式终端使用 `--rviz` 时，
脚本会在抓取前和成功后各等待一次 Enter，方便检查初始/最终状态和切换三层
可视化；非交互调用会自动跳过等待。

## 1. Codex 接手后的第一步

请先完整阅读本文件，然后：

1. 用简短语言复述当前已经确定的结论；
2. 指出下一步最值得决定的一个问题；
3. 暂时不要写代码，等用户确认方案后再实施。

与用户讨论时遵循以下风格：

- 结论先行，用尽量少的话表达核心信息；
- 一次只展开一个层级或一个关键决策，避免信息过载；
- 先对齐 high-level 方案，再进入 package、node、消息字段等实现细节；
- 优先复用 ROS 2 现有生态，不重复造轮子；
- 如果要改变本文中的既定结论，必须先明确指出原因并与用户讨论；
- 不要把探索过程中已经否定的方案重新当成当前方案。

## 2. 项目目标与系统边界

目标是在约 10 平方米区域内，由 Unitree Go2 搭载 Unitree D1-550 机械臂，自动拾取随机散落的 12 件垃圾并投入固定垃圾桶。

系统总体流程：

1. Go2 自主移动到某件垃圾附近；
2. Go2 侧的总流程调用机械臂子系统的 `Pick` 高层接口；
3. 机械臂利用 eye-in-hand RealSense D435i 主动调整视角、精定位并完成抓取和举起；
4. Go2 携带垃圾移动到垃圾桶附近；
5. Go2 侧调用机械臂子系统的 `Place` 高层接口；
6. 机械臂完成投放并收回，Go2 继续处理下一件垃圾。

机械臂子系统并不只是机械臂驱动，它内部包含：相机、感知、主动视角调整、运动规划、夹爪控制和抓取流程管理。Go2 侧不应直接操纵机械臂关节或夹爪。

### 硬件与已知约束

- 移动平台：Unitree Go2；
- 机械臂：Unitree D1-550；
- 相机：RealSense D435i，eye-in-hand 安装；
- 真机部署时，机械臂和 D435i 都连接到 Go2 扩展坞，由机器狗端运行并控制整体流程；
- 夹爪净开口约 6.5 cm；
- 第一版明确规定：执行 `Pick` 或 `Place` 时，Go2 必须站稳并保持不动；
- 机械臂必须自主感知和决定抓取位姿，而不是依赖人工给出精确物体位姿。

### 三类目标物体

1. 黄色立方体：边长 5 cm；
2. 绿色西葫芦：长条形，可能有一定弯曲；
3. 碗：始终开口朝上，碗口直径约 11.5 cm。

## 3. 核心感知思路：两阶段主动感知

已经确定的核心原则是：

> 第一次斜视观测只负责粗定位；机械臂随后把 eye-in-hand 相机移动到目标正上方，第二次俯视观测再完成精确抓取位姿估计。

这是整个抓取方案最关键的简化。不要优先尝试在任意斜视角下直接恢复完整 6D 位姿。固定类别、已知几何且能主动换视角时，俯视后的二维 mask 几何通常已经足够；深度主要用于粗定位和确定抓取高度。

通用流程：

1. Go2 到位并站稳；
2. D435i 获取 RGB-D；
3. 分割目标并获得粗位置；
4. 机械臂将相机移动到目标正上方；
5. 必要时通过图像中心误差继续微调，使目标居中；
6. 根据物体类别运行专用的俯视位姿估计算法；
7. 规划并执行抓取；
8. 验证结果并举起。

## 4. 三类物体的当前抓取方案

### 4.1 立方体

粗定位：从相机中心经过分割 mask 中心发射三维射线，与离地 2.5 cm 的水平面相交，将交点作为立方体中心的近似值。斜视下 mask 中心不等于真实立方体中心，但作为移动到俯视观察位的粗定位已经足够。

俯视精定位：

1. 相机光轴对准 `-world_z`；
2. 重新分割立方体并使其位于图像中心附近；
3. 对 mask 轮廓做四边形拟合或 `minAreaRect`；
4. 从正方形边方向估计 yaw，yaw 只需在模 90°意义下确定；
5. 夹爪闭合方向与立方体的一组边对齐，执行 top-down 抓取。

关键约束：夹爪开口 6.5 cm，立方体边长 5 cm，但面对角线约 7.07 cm，因此不能忽略 yaw 后随意夹取。俯视后估计边方向正是为了解决这个问题。

### 4.2 西葫芦

移动到西葫芦正上方后：

1. 用 HSV/Lab 颜色阈值提取绿色像素，并做简单形态学去噪；
2. 西葫芦近似笔直，可对 mask 像素做 PCA 得到长轴, 然后提取骨架，并仅在骨架中部的一小段估计局部长轴，避免全局 PCA 给出错误抓取方向；
3. approach vector 取向下的地面法向 `-world_z`；
4. closing vector 取水平面内垂直于局部长轴的方向，可由地面法向与长轴叉乘得到；
5. 抓取位置取物体中部附近，抓取高度由深度、地面高度、物体半径和 TCP 偏置共同确定。

注意：物体中心/抓取目标点不是 TCP。TCP 是固定在夹爪上的工具坐标系。

### 4.3 碗

碗始终开口向上，因此先移动到碗正上方，再拟合俯视圆形轮廓：

1. 重新分割碗；
2. 对碗口圆或稳定可见的圆形轮廓做鲁棒圆拟合；
3. 得到圆心和半径；
4. 碗具有旋转对称性，不需要估计 yaw。

但圆心只是定位基准，不一定是最终夹爪中心。由于碗口直径约 11.5 cm，大于夹爪开口 6.5 cm，当前考虑“一指在碗内、一指在碗外”夹住碗沿：

- 在圆周上选择一个碗沿抓取点；
- closing direction 沿该点的径向；
- 夹爪中心对准碗沿，而不是碗心。

这一机械抓取方式需要优先通过简单真机实验验证稳定性。若不可行，应先调整末端执行器或抓取方式，而不是继续堆视觉算法。

## 5. ROS 2 的 high-level 决策

### 5.1 ROS 2 是系统集成骨架

本项目从开发阶段起就基于 ROS 2 组织，因为最终必须部署到真机，并且需要整合 Go2、D1-550、D435i、感知、规划和任务流程。

ROS 2 主要负责：

- 模块通信与任务调度；
- TF 和时间戳管理；
- RViz2 可视化；
- rosbag2 数据录制与复现；
- 对接 MoveIt 2 等规划工具；
- 让仿真与真机共享上层接口。

机械臂的高频伺服、SDK 通信、看门狗和底层安全逻辑仍应留在 D1 驱动/厂商 SDK 层，不应依赖普通 ROS 2 Python 节点实现硬实时控制。

### 5.2 优先复用 ROS 2 生态

优先考虑使用：

- `tf2`；
- RViz2；
- MoveIt 2；
- `robot_state_publisher`；
- rosbag2；
- RealSense 官方 ROS 2 驱动；
- Go2 的 ROS 2 接入。

是否从第一版就实现完整的 `ros2_control` hardware interface 尚未决定，需要先检查 D1 SDK 能力。不要为了形式完整而过早增加复杂度。

### 5.3 MuJoCo 与真机共用上层接口

开发期间必须能连接 MuJoCo 验证算法。架构原则是：

```text
感知/抓取/任务逻辑
        ↓
统一的 ROS 2 接口
        ↓
MuJoCo 驱动 或 D1 真机驱动
```

上层 manipulation 算法不应知道当前连接的是 MuJoCo 还是真机；只替换底层驱动和必要配置。需要特别避免在算法代码中直接调用 D1 SDK 或 MuJoCo API。

仿真与真机可以在传感器噪声、控制频率和安全限制上使用不同参数，但接口语义应保持一致。

### 5.4 Go2 与机械臂子系统的职责边界

Go2 侧负责：

- 自主搜索/导航；
- 判断已接近垃圾或垃圾桶；
- 保证调用机械臂任务前已经站稳；
- 调用机械臂子系统的高层任务接口；
- 根据任务结果决定继续、重试或放弃。

机械臂子系统至少暴露两个长任务接口，倾向使用 ROS 2 Action：

- `Pick`：主动观察 → 精定位 → 规划 → 抓取 → 举起；
- `Place`：到垃圾桶附近后执行放置/投放 → 收回机械臂。

它们应返回执行阶段、成功/失败以及明确的失败原因。具体 Action 字段目前尚未设计，不要现在就锁死。

## 6. 尚未决定的问题

下面这些问题仍需继续讨论。请按优先级一次讨论一个，不要一次全部展开。

### 当前最优先

1. ROS 2 系统应拆成哪些 package 和 node；
2. MuJoCo 驱动与 D1 真机驱动应共用什么标准控制接口；
3. `Pick` / `Place` 与底层轨迹控制的职责边界。

### 需要 D1 SDK 信息后决定

4. D1 驱动是简单 SDK wrapper，还是实现完整 `ros2_control` hardware interface；
5. SDK 接收完整轨迹还是周期性单点目标，插值应放在哪一层；
6. 关节状态、使能、故障、停止和夹爪分别如何映射到 ROS 2 接口；
7. 控制频率、反馈频率、时间戳与通信看门狗设计。

### 后续再决定

8. TF 树的最终 frame 命名与静态/动态变换发布者；
9. MoveIt 2 的规划组、碰撞模型和规划执行链；
10. RealSense QoS、RGB-D 对齐与时间同步；
11. rosbag2 记录范围和实验日志格式；
12. 抓取成功检测、失败重试与错误码；
13. `Place` 是使用固定垃圾桶相对位姿，还是也需要视觉精定位；
14. Go2 是否未来允许在抓取期间配合调整机身高度或姿态。

## 8. 下一轮讨论建议

下一轮只讨论这一个问题：

> 为了让 MuJoCo 和 D1 真机共用上层抓取算法，ROS 2 工程最小应拆成哪些 package/node，各层之间的责任是什么？

讨论时先给出最小可用架构和选择理由，不要立刻展开消息字段、QoS、launch 文件或完整代码。用户确认模块边界后，再进入接口设计。

为了做出可靠决定，后续最好向用户索取并阅读：

- 当前工程目录结构（如果已经存在）；
- D1-550 SDK、文档和示例代码；
- 机械臂 URDF/描述文件现状；
- 计划使用的 Ubuntu、ROS 2、MuJoCo 版本；
- Go2 扩展坞计算平台、CPU 架构与系统环境；
- 夹爪控制接口；
- 相机实际安装方式和手眼标定进展。

## 9. 参考入口

- Unitree ROS 2：<https://github.com/unitreerobotics/unitree_ros2>
- RealSense ROS：<https://github.com/realsenseai/realsense-ros>

这些入口可能随版本更新。涉及具体安装命令、兼容版本或 API 时，应重新核对官方资料，而不是只依赖本文。
