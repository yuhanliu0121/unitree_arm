# 真实 RGB-D 验证样例

来源：冻结的 `real_test_camera_v1`，帧 `cap_20260801_082106_0001`。

- RGB 模型共输出 11 个实例。
- 深度/尺寸过滤保留画面中的 10 个真实目标。
- 右侧电缆被 RGB 模型误识别为 `bowl`，但其投影物理尺寸约 29.8 cm，超过碗的允许范围，因此状态为 `rejected`。
- `visualization.jpg` 左侧为 RGB 分割和状态，右侧为对齐深度。
- `objects.json` 是同一帧的结构化接口输出。

这只是运行链路的固定回归样例，不代表完整真实测试集统计。
